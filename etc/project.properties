build.dir=build
etc.dir=etc
lib.dir=lib
src.dir=src
src.bin.dir=${src.dir}/bin

dist.build.dir=${build.dir}/dist

main.jar.name=main.jar
main.jar.build.dir=${build.dir}/${main.jar.name}

admin.jar.name=eSciDocCoreAdmin.jar
admin.jar.build.dir=${build.dir}/${admin.jar.name}
admin.jar.main.dir=${admin.jar.build.dir}/main
admin.jar.lib.dir=${admin.jar.build.dir}/lib

bin.src.dir=${src.dir}/bin
doc.src.dir=${basedir}/doc
sql.src.dir=${basedir}/data/sql

java.build.dir=${build.dir}/classes
java.src.dir=${src.dir}/java
test.src.dir=${src.dir}/test

metadata.src.dir=${src.dir}/metadata

junit.result.dir=${build.dir}/junit
junit.xml.result.dir=${junit.result.dir}/xml
junit.failed.file=${junit.result.dir}/failed
junit.html.result.dir=${junit.result.dir}/html

